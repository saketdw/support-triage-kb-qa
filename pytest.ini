[pytest]
# Put the repo root on sys.path so `pytest` (the bare binary, as CI runs it)
# can import the local packages. Without this, only `python -m pytest` works,
# because that form implicitly adds the working directory.
pythonpath = .
testpaths = tests
